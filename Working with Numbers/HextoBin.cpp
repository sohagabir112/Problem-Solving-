//Hexadecimal to binary conversion
#include<iostream>
#include<math.h>
using namespace std;

void convertBinary(string hex){
    long long bin =0;
    int i=0,count=1,pos=0;
    
     
}

int main(){
    string hex;
    cin>>hex;
    convertBinary(hex);

    return 0;
}