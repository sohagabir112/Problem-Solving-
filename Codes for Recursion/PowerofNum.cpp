// Power of a number 
#include<iostream>
#include<math.h>
using namespace std;

int main(){
    int n, p;
    cout<<"Enter number and power in sequence: ";
    cin>>n>>p;
    cout<<pow(n,p);
    return 0;
}